# Normalise to LF in the repository. The same tree is checked out on a Windows
# laptop and a Raspberry Pi, and CRLF creeping into source would make every
# cross-platform diff unreadable.
* text=auto eol=lf
*.png binary
*.jpg binary
